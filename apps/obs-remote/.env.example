# Local development only. Production builds must use HTTPS/WSS.
# Native iOS/Android cannot trust the portless *.visp.localhost cert — use HTTP.
# Simulator: http://127.0.0.1:3000  |  Physical device: http://LAN_IP:3000
EXPO_PUBLIC_SERVER_URL=http://127.0.0.1:3000
